import { Router } from "express";

const router = Router();

// Auth is handled entirely by Clerk — no custom auth endpoints needed.
// This file is kept for any future auth-related utility routes.

export default router;
