{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "PORT=${PORT:-8080} BASE_PATH=${BASE_PATH:-/} pnpm --filter @workspace/questmaster dev",
    "build": "pnpm -r --filter \"./artifacts/**\" --if-present run build",
    "typecheck:libs": "echo 'skipping lib typecheck'",
    "typecheck": "pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "@lovable.dev/cloud-auth-js": "^1.1.2",
    "@replit/connectors-sdk": "^0.4.0"
  }
}
